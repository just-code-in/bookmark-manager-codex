# Runtime config
NODE_ENV=development
PORT=4040

# Secrets
# This project currently does not require any external API keys or tokens.
# Add new secret variables here with placeholder values when integrations are introduced.
